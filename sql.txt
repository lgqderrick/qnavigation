
 CREATE TABLE IF NOT EXISTS `user_tbl`(   //建表
   user_id INT UNSIGNED AUTO_INCREMENT not null,
   username VARCHAR(100) NOT NULL,
   password VARCHAR(40) NOT NULL,
   nickName varchar(100) not null,
   PRIMARY KEY ( user_id)
);

insert into user_tbl (username,password,nickName,vehicle_id) values('c','c','小蓝',3);
alter table user_tbl add nickName varchar(100) not null;
alter table user_tbl add phone varchar(100);
alter table user_tbl add busy int DEFAULT '0';
 update user_tbl set nickName="小芳" where user_id=1;
update user_tbl set  phone="15817378437"
-----------------------------------------------------------
 CREATE TABLE IF NOT EXISTS vehicle_tbl( 
   vehicle_id INT UNSIGNED AUTO_INCREMENT not null,
   user_id INT UNSIGNED NOT NULL,
   size INT NOT NULL,
   policy INT NOT NULL,
   height DOUBLE(16,2)  NOT NULL,
   width DOUBLE(16,2)  NOT NULL,
   lenght   DOUBLE(16,2) NOT NULL,
   weight DOUBLE(16,2) NOT NULL,
   PRIMARY KEY ( vehicle_id)
);
insert into vehicle_tbl values(1,1,1,1,3.1,2.1,5.9,5.4);
alter table vehicle rename vehicle_tbl
修改字段名
alter table vehicle_tbl change length length double(16,2) not null;